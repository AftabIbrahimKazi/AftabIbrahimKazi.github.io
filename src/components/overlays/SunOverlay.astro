---
// src/components/overlays/SunOverlay.astro — About / Identity
// 8 sections — layout data-attributes drive CSS positioning.
// Sections match SUN_SCROLL_PATH waypoints in PlanetScrollCameras.ts.

const items = [
  {
    id:       'discipline',
    category: 'DISCIPLINE',
    title:    'What I Build',
    meta:     'Full-stack · Design to deployment',
    body: [
      'I work across the full product lifecycle — from early research and wireframing through component-level design and shipped code. The split between "designer" and "developer" has never resonated. The most interesting problems live at the boundary.',
      'On the design side: UI/UX for web and mobile, design systems from scratch, motion and interaction, brand and visual identity. On the engineering side: semantic HTML, accessible CSS, TypeScript, React, Astro, Three.js, GSAP.',
      'I am most effective on small, focused teams where design and engineering work closely together — where a decision in Figma is immediately stress-tested against what is possible in code, and vice versa.',
    ],
    topics: ['The Boundary', 'Design + Engineering', 'Small Teams'],
  },
  {
    id:       'philosophy',
    category: 'PHILOSOPHY',
    title:    'How I Work',
    meta:     'Problem-first · Clarity over cleverness',
    body: [
      'Design starts with constraints, not aesthetics. Before touching Figma, I want to understand: what problem are we solving, for whom, and what does success look like? The visual language is a response to those answers — not a starting point.',
      'Clarity over cleverness is the rule that has saved me the most time. A clever solution nobody else can maintain is a liability. A clear solution another developer can read and modify without asking questions is a gift. I write code and design systems with the next person in mind.',
      'I work async-first, with documented decisions and clean handoffs. I will tell you when a timeline is unrealistic, when a brief needs more clarity, or when a technical decision I disagree with is about to cause a problem.',
    ],
    topics: ['Constraints First', 'Clarity Over Cleverness', 'Async-First'],
  },
  {
    id:       'stack',
    category: 'STACK',
    title:    'Tools of the Trade',
    meta:     'Design + engineering stack',
    body: [
      'Design tools: Figma for UI and systems. Illustrator for vector and identity. After Effects and GSAP for motion. Framer for interactive prototyping when static wireframes are not enough.',
      'Frontend: TypeScript with strict mode. React for component-driven UIs. Astro for content-focused sites. GSAP for animation. Three.js and GLSL for 3D and shader work. CSS with cascade layers and custom properties — no framework dependency.',
      'Backend and infrastructure: Node.js, PostgreSQL, Supabase. Vercel and Netlify for deployment. I am comfortable owning the full stack on smaller projects and contributing to backend systems on larger ones.',
    ],
    topics: ['Design Tools', 'Frontend', 'Backend + Infra'],
  },
  {
    id:       'highlights',
    category: 'HIGHLIGHTS',
    title:    'At a Glance',
    meta:     'Key numbers',
    body: [
      '12+ client and personal projects shipped across design and engineering, spanning healthcare, fintech, e-commerce, and creative-tech work. Every project on this site is a stop on that list — Venus, Earth, and Mars each hold a different slice of it.',
      'Specificity is the point. "Years of experience" says less than "shipped a design system that is still the default starting point for a team\'s new features two hundred days later" — the kind of outcome that is actually worth judging work by.',
      'What comes next is more of the same: small, focused teams, problems that sit at the design/engineering boundary, and work that holds up under scrutiny — not just at first glance.',
    ],
    topics: ['The Numbers', 'Why Specificity Matters', "What's Next"],
  },
];
---

<!-- ── Section 1 — Intro ──────────────────────────────────── -->
<div class="ex-sun-section" data-layout="bottom-center">
  <span class="ex-sun-section-label">THE SOURCE</span>
  <h2 class="ex-heading ex-sun-section-title">Everything<span class="ex-line-break"></span>Orbits Something.</h2>
  <p class="ex-para ex-sun-body">I'm Aftab Kazi — a designer and developer building digital products at the intersection of craft and clarity. This is where the system starts. Freelance, open to full-time, passionate about work that matters.</p>
  <div class="ex-sun-stats-row">
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">5+ yrs</span><span class="ex-sun-stat-pill-label">Experience</span></div>
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">Design + Dev</span><span class="ex-sun-stat-pill-label">Discipline</span></div>
  </div>
  <button class="ex-sun-intro-cta" type="button" data-scroll-next>
    EXPLORE THE SOURCE
    <span class="ex-sun-intro-cta-arrow"><i class="fa-solid fa-arrow-right"></i></span>
  </button>
</div>

<!-- ── Section 2 — Discipline ──────────────────────────────── -->
<div class="ex-sun-section" data-layout="right">
  <span class="ex-sun-section-label">02 — DISCIPLINE</span>
  <h2 class="ex-heading ex-sun-section-title">What I Build</h2>
  <p class="ex-para ex-sun-body">UI/UX design. Front-end engineering. Design systems. Motion and interaction. I work across the full product lifecycle — from early concepts to shipped code. Comfortable in Figma and a terminal in equal measure.</p>
  <div class="ex-sun-key-fact">
    <span class="ex-sun-fact-number">Full-Stack</span>
    <span class="ex-sun-fact-label">Design to deployment</span>
  </div>
  <button class="ex-sun-detail-link" type="button" data-item-id="0" data-planet="sun">More detail →</button>
</div>

<!-- ── Section 3 — Philosophy ──────────────────────────────── -->
<div class="ex-sun-section" data-layout="right">
  <span class="ex-sun-section-label">03 — PHILOSOPHY</span>
  <h2 class="ex-heading ex-sun-section-title">How I Work</h2>
  <p class="ex-para ex-sun-body">Clarity over cleverness. Systems over one-offs. I believe the best design is invisible — it removes friction and lets the work speak. I start with constraints, not aesthetics. The visual language follows from the problem.</p>
  <div class="ex-sun-key-fact">
    <span class="ex-sun-fact-number">Problem-first</span>
    <span class="ex-sun-fact-label">Design methodology</span>
  </div>
  <button class="ex-sun-detail-link" type="button" data-item-id="1" data-planet="sun">More detail →</button>
</div>

<!-- ── Section 4 — Stack ────────────────────────────────────── -->
<div class="ex-sun-section" data-layout="right">
  <span class="ex-sun-section-label">04 — STACK</span>
  <h2 class="ex-heading ex-sun-section-title">Tools of the Trade</h2>
  <p class="ex-para ex-sun-body">Figma, Astro, TypeScript, React, GSAP, Three.js, CSS. On the back end: Node, PostgreSQL, Supabase. I choose tools that match the problem — not the trend.</p>
  <div class="ex-sun-stats-row">
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">Figma</span><span class="ex-sun-stat-pill-label">Design</span></div>
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">Astro / TS</span><span class="ex-sun-stat-pill-label">Frontend</span></div>
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">GSAP</span><span class="ex-sun-stat-pill-label">Motion</span></div>
  </div>
  <button class="ex-sun-detail-link" type="button" data-item-id="2" data-planet="sun">Full stack breakdown →</button>
</div>

<!-- ── Section 5 — Background ──────────────────────────────── -->
<div class="ex-sun-section" data-layout="right">
  <span class="ex-sun-section-label">05 — BACKGROUND</span>
  <h2 class="ex-heading ex-sun-section-title">The Journey</h2>
  <p class="ex-para ex-sun-body">Started as a self-taught developer, picking apart other people's code long before writing much of my own. Freelance from day one — no bootcamp, no internship, just shipped work and steadily harder problems. Picked up 3D and creative coding the same way: by building this planetarium.</p>
  <div class="ex-sun-key-fact">
    <span class="ex-sun-fact-number">Since 2020</span>
    <span class="ex-sun-fact-label">Working independently</span>
  </div>
</div>

<!-- ── Section 6 — Values ───────────────────────────────────── -->
<div class="ex-sun-section" data-layout="right">
  <span class="ex-sun-section-label">06 — VALUES</span>
  <h2 class="ex-heading ex-sun-section-title">What I Stand By</h2>
  <p class="ex-para ex-sun-body">Honest timelines. Clean handoffs. Code that another human can read. I don't over-promise and I don't disappear mid-project. I collaborate openly and I'll tell you when something isn't working — before it ships.</p>
  <div class="ex-sun-stats-row">
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">Async-first</span><span class="ex-sun-stat-pill-label">Communication</span></div>
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">Open source</span><span class="ex-sun-stat-pill-label">Contributor</span></div>
  </div>
</div>

<!-- ── Section 7 — Highlights ───────────────────────────────── -->
<div class="ex-sun-section" data-layout="right">
  <span class="ex-sun-section-label">07 — HIGHLIGHTS</span>
  <h2 class="ex-heading ex-sun-section-title">At a Glance</h2>
  <p class="ex-para ex-sun-body">Projects shipped, clients worked with, and the range of problems tackled along the way — the numbers that matter most when deciding who to work with next.</p>
  <div class="ex-sun-key-fact">
    <span class="ex-sun-fact-number">12+ Projects</span>
    <span class="ex-sun-fact-label">Shipped across 4 industries</span>
  </div>
  <button class="ex-sun-detail-link" type="button" data-item-id="3" data-planet="sun">Full highlights →</button>
</div>

<!-- ── Section 8 — Navigate ─────────────────────────────────── -->
<div class="ex-sun-section" data-layout="center">
  <span class="ex-sun-section-label">08 — THE SYSTEM</span>
  <h2 class="ex-heading ex-sun-section-title">Explore the Orbits</h2>
  <p class="ex-para ex-sun-body">Each planet in this system holds a different body of work. Design lives on Venus. Development on Earth. Experiments on Mars. Enterprise work on Jupiter. Products on Saturn. Concepts on Uranus. Reach me at Neptune.</p>
  <div class="ex-sun-stats-row">
    <div class="ex-sun-stat-pill"><span class="ex-sun-stat-pill-value">8 planets</span><span class="ex-sun-stat-pill-label">8 bodies of work</span></div>
  </div>
</div>

<!-- ── Item data for article panel ──────────────────────────── -->
<script type="application/json" id="ex-sun-items-data" is:inline set:html={JSON.stringify(items)}></script>

<script>
  import StrataOffcanvas from '@strata-packages/offcanvas';
  import { SunProgressStrip } from '../../scripts/ui/solar-system/SunProgressStrip';

  function init(): void {
    new SunProgressStrip().init();

    document.querySelector<HTMLElement>('[data-scroll-next]')?.addEventListener('click', () => {
      const sections = document.querySelectorAll<HTMLElement>('.ex-sun-section');
      sections[1]?.scrollIntoView({ behavior: 'smooth', block: 'center' });
    });

    const dataEl   = document.getElementById('ex-sun-items-data');
    const panel    = document.getElementById('ex-sun-article-panel');
    const closeBtn = panel?.querySelector<HTMLElement>('[data-st-dismiss="offcanvas"]') ?? null;

    if (!dataEl || !panel) return;

    const catEl   = document.getElementById('ex-sun-article-panel-cat');
    const titleEl = document.getElementById('ex-sun-article-panel-title');
    const metaEl  = document.getElementById('ex-sun-article-panel-meta');
    const bodyEl  = document.getElementById('ex-sun-article-panel-body');

    const items: { id: string; category: string; title: string; meta: string; body: string[]; topics: string[] }[] =
      JSON.parse(dataEl.textContent ?? '[]');

    const openItem = (item: typeof items[number]): void => {
      if (catEl)   catEl.textContent   = item.category;
      if (titleEl) titleEl.textContent = item.title;
      if (metaEl)  metaEl.textContent  = item.meta;
      if (bodyEl)  bodyEl.innerHTML    = item.body
        .map((para, i) => `<p data-topic="${(item.topics[i] ?? '').replace(/"/g, '&quot;')}">${para}</p>`)
        .join('');

      StrataOffcanvas.open(panel);
      setTimeout(() => closeBtn?.focus(), 60);
    };

    // The package fires these regardless of how the panel was dismissed
    // (close button, backdrop, Escape) — relaying them tells the progress
    // strip when to switch between section and article-paragraph pagination.
    document.addEventListener('st:offcanvas:open', (e: Event) => {
      if ((e as CustomEvent).detail?.offcanvas !== panel) return;
      window.dispatchEvent(new CustomEvent('ct:sun-article-open'));
    });
    document.addEventListener('st:offcanvas:close', (e: Event) => {
      if ((e as CustomEvent).detail?.offcanvas !== panel) return;
      window.dispatchEvent(new CustomEvent('ct:sun-article-close'));
    });

    document.addEventListener('click', (e: MouseEvent) => {
      const btn = (e.target as Element).closest<HTMLElement>('[data-item-id]');
      if (!btn || btn.dataset.planet !== 'sun') return;
      const item = items[parseInt(btn.dataset.itemId ?? '', 10)];
      if (!item) return;
      openItem(item);
    });
  }

  init();
</script>
