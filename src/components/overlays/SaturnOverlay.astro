---
// src/components/overlays/SaturnOverlay.astro — Products / Store

const items = [
  {
    category: 'PRODUCT',
    title:    '[Product Name]',
    meta:     '[Price] · [Platform]',
    body: [
      'Placeholder — describe your flagship product. What is it, who is it for, and what makes it worth buying? A theme, a Figma kit, a component library, an icon set — be specific about the format and what is included.',
      'What problem does this product solve? The best digital products have a clear job to be done. Describe the specific situation a buyer is in when this product is exactly what they need. That specificity is what turns a visitor into a customer.',
      'What is included? Document the contents: number of components, file formats, included variants, update access, support terms. Buyers at this stage are close to purchasing — they need detail, not persuasion. Update this placeholder with your actual product.',
    ],
  },
  {
    category: 'PRODUCT',
    title:    '[Product Name]',
    meta:     '[Price] · Includes [N] assets',
    body: [
      'Placeholder — a second product. This could be a template system, a motion pack, or a documentation framework. Different format, different audience, different use case from your first product.',
      'Who is this for? The more specific the target buyer, the better the product sells. "Designers who use Figma for SaaS products" is more useful than "designers." Define your audience and address them directly in this description.',
      'What does the buyer walk away with immediately? Describe the experience of downloading and opening this product. Is it plug-and-play or does it require configuration? Is there documentation? What does the onboarding look like? Update this placeholder.',
    ],
  },
  {
    category: 'PRODUCT',
    title:    '[Product Name]',
    meta:     '[Price] · [N] files included',
    body: [
      'Placeholder — a third product. Icon pack, Notion template, email kit, Webflow template. Each product in your catalogue should serve a different need so buyers have a reason to purchase multiple items.',
      'What differentiates this from what is already available? Crowded markets require a clear differentiator. Describe what makes this product the right choice over existing alternatives — quality, scope, specificity, aesthetic, or price.',
      'What is the licence? Commercial use, personal use, extended licence for client work? Licence terms are a purchase blocker — be clear upfront. Describe the exact terms and any restrictions. Update this placeholder with your actual product details.',
    ],
  },
];
---

<!-- 01 — Overview -->
<div class="ex-saturn-section" data-layout="bottom-center">
  <span class="ex-saturn-section-label">01 — Products</span>
  <h2 class="ex-saturn-section-title ex-heading">Things for Sale</h2>
  <p class="ex-para ex-saturn-body">Themes, templates, UI kits, and digital assets built for other designers and developers. Products that encode the same standards I hold in client work. Placeholder — update with your actual product catalogue.</p>
  <div class="ex-saturn-stats-row">
    <div class="ex-saturn-stat-pill"><span class="ex-saturn-stat-pill-value">[N] products</span><span class="ex-saturn-stat-pill-label">Available</span></div>
    <div class="ex-saturn-stat-pill"><span class="ex-saturn-stat-pill-value">[Platform]</span><span class="ex-saturn-stat-pill-label">Where to buy</span></div>
  </div>
</div>

<!-- 02 — Product 1 -->
<div class="ex-saturn-section" data-layout="bottom-right">
  <span class="ex-saturn-section-label">02 — Product</span>
  <h2 class="ex-heading ex-saturn-section-title">[Product Name]</h2>
  <p class="ex-para ex-saturn-body">Placeholder — describe your flagship product. What it is, who it's for, what makes it worth buying. A theme, a Figma kit, a component library, an icon set. Describe the value clearly.</p>
  <div class="ex-saturn-key-fact">
    <span class="ex-saturn-fact-number">[Price]</span>
    <span class="ex-saturn-fact-label">One-time purchase</span>
  </div>
  <button class="ex-saturn-detail-link" data-item-id="0" data-item-side="left" data-planet="saturn">Product details →</button>
</div>

<!-- 03 — Product 2 -->
<div class="ex-saturn-section" data-layout="top-left">
  <span class="ex-saturn-section-label">03 — Product</span>
  <h2 class="ex-heading ex-saturn-section-title">[Product Name]</h2>
  <p class="ex-para ex-saturn-body">Placeholder — a second product. Could be a template system, a motion pack, or a documentation framework. Describe what's included, what problem it solves, and who it's designed for.</p>
  <div class="ex-saturn-key-fact">
    <span class="ex-saturn-fact-number">[Price]</span>
    <span class="ex-saturn-fact-label">Includes [N] assets</span>
  </div>
  <button class="ex-saturn-detail-link" data-item-id="1" data-item-side="right" data-planet="saturn">Product details →</button>
</div>

<!-- 04 — Product 3 -->
<div class="ex-saturn-section" data-layout="right">
  <span class="ex-saturn-section-label">04 — Product</span>
  <h2 class="ex-heading ex-saturn-section-title">[Product Name]</h2>
  <p class="ex-para ex-saturn-body">Placeholder — a third product. Icon pack, Notion template, email kit, Webflow template. Describe the scope and what differentiates it from what's already out there.</p>
  <div class="ex-saturn-stats-row">
    <div class="ex-saturn-stat-pill"><span class="ex-saturn-stat-pill-value">[N] files</span><span class="ex-saturn-stat-pill-label">Included</span></div>
    <div class="ex-saturn-stat-pill"><span class="ex-saturn-stat-pill-value">[Format]</span><span class="ex-saturn-stat-pill-label">File type</span></div>
  </div>
  <button class="ex-saturn-detail-link" data-item-id="2" data-item-side="left" data-planet="saturn">Product details →</button>
</div>

<!-- 05 — Offer -->
<div class="ex-saturn-section" data-layout="bottom-left">
  <span class="ex-saturn-section-label">05 — Offer</span>
  <h2 class="ex-heading ex-saturn-section-title">What's Included</h2>
  <p class="ex-para ex-saturn-body">All products come with documentation, update access, and a licence for commercial use. Placeholder — describe your licensing, update policy, and any support you offer with products.</p>
  <div class="ex-saturn-key-fact">
    <span class="ex-saturn-fact-number">Commercial</span>
    <span class="ex-saturn-fact-label">Licence included</span>
  </div>
</div>

<!-- 06 — Custom Work -->
<div class="ex-saturn-section" data-layout="top-left">
  <span class="ex-saturn-section-label">06 — Custom</span>
  <h2 class="ex-heading ex-saturn-section-title">Need Something Bespoke?</h2>
  <p class="ex-para ex-saturn-body">Products are starting points. If you need a theme modified, a template system extended, or a design kit built to your brand, I offer custom product development. Placeholder — update with your custom terms.</p>
  <div class="ex-saturn-key-fact">
    <span class="ex-saturn-fact-number">From [Price]</span>
    <span class="ex-saturn-fact-label">Custom product work</span>
  </div>
</div>

<!-- 07 — Reviews -->
<div class="ex-saturn-section" data-layout="right">
  <span class="ex-saturn-section-label">07 — Reviews</span>
  <h2 class="ex-heading ex-saturn-section-title">What Customers Say</h2>
  <p class="ex-para ex-saturn-body">Placeholder — add a customer review or two here. Real words from real buyers carry more weight than any copy you write about your own products. Aggregate rating, number of sales, top review snippet.</p>
  <div class="ex-saturn-key-fact">
    <span class="ex-saturn-fact-number">[Rating] / 5</span>
    <span class="ex-saturn-fact-label">[N] reviews</span>
  </div>
</div>

<!-- 08 — Shop CTA -->
<div class="ex-saturn-section" data-layout="center">
  <span class="ex-saturn-section-label">08 — Shop</span>
  <h2 class="ex-heading ex-saturn-section-title">Browse the Store</h2>
  <p class="ex-para ex-saturn-body">All products are available on [Platform — Gumroad, Lemonsqueezy, etc.]. Instant download after purchase. Questions before buying? Reach out at Neptune and I'll answer the same day.</p>
  <div class="ex-saturn-stats-row">
    <div class="ex-saturn-stat-pill"><span class="ex-saturn-stat-pill-value">[Platform]</span><span class="ex-saturn-stat-pill-label">Store</span></div>
    <div class="ex-saturn-stat-pill"><span class="ex-saturn-stat-pill-value">Instant</span><span class="ex-saturn-stat-pill-label">Delivery</span></div>
  </div>
</div>

<!-- ── Item data for sidebar ───────────────────────────────────── -->
<script type="application/json" id="ex-saturn-items-data" is:inline set:html={JSON.stringify(items)}></script>

<script>
  import StrataOffcanvas from '@strata-packages/offcanvas';

  function init(): void {
    const dataEl       = document.getElementById('ex-saturn-items-data');
    const sidebarLeft  = document.getElementById('ex-saturn-sidebar-left');
    const sidebarRight = document.getElementById('ex-saturn-sidebar-right');

    if (!dataEl || !sidebarLeft || !sidebarRight) return;

    const closeBtnLeft  = sidebarLeft.querySelector<HTMLElement>('[data-st-dismiss="offcanvas"]');
    const closeBtnRight = sidebarRight.querySelector<HTMLElement>('[data-st-dismiss="offcanvas"]');

    const items: { category: string; title: string; meta: string; body: string[] }[] =
      JSON.parse(dataEl.textContent ?? '[]');

    document.addEventListener('click', (e: MouseEvent) => {
      const btn = (e.target as Element).closest<HTMLElement>('[data-item-id]');
      if (!btn || btn.dataset.planet !== 'saturn') return;
      const item = items[parseInt(btn.dataset.itemId ?? '', 10)];
      if (!item) return;

      const side     = btn.dataset.itemSide === 'right' ? 'right' : 'left';
      const sidebar  = side === 'right' ? sidebarRight : sidebarLeft;
      const closeBtn = side === 'right' ? closeBtnRight : closeBtnLeft;

      const catEl   = document.getElementById(`ex-saturn-sidebar-cat-${side}`);
      const titleEl = document.getElementById(`ex-saturn-sidebar-title-${side}`);
      const metaEl  = document.getElementById(`ex-saturn-sidebar-meta-${side}`);
      const bodyEl  = document.getElementById(`ex-saturn-sidebar-body-${side}`);

      if (catEl)   catEl.textContent   = item.category;
      if (titleEl) titleEl.textContent = item.title;
      if (metaEl)  metaEl.textContent  = item.meta;
      if (bodyEl)  bodyEl.innerHTML    = item.body.map(p => `<p>${p}</p>`).join('');

      StrataOffcanvas.open(sidebar);
      setTimeout(() => closeBtn?.focus(), 60);
    });
  }

  init();
</script>
